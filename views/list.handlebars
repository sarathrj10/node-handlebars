<h4>Items as list</h4>
<div class="row ml-3">
    <ul class="list-group">
        {{#each items}}
        <li class="list-group-items mt-3">{{this.name}}</li>
        <li class="list-group-items">Price :{{this.price}}</li>
        <li class="list-group-items">Quantity :{{this.quantity}}</li>
        {{/each}}
    </ul>
</div>
<a href="/"><button type="button" class="my-3 btn btn-primary">View as Table</button></a>
<a href="/cards"><button type="button" class="my-3 btn btn-primary">View as Cards</button></a>